version: "3.8"

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: chineselaw-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: chineselaw
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: chineselaw-redis
    ports:
      - "6379:6379"
    restart: unless-stopped

volumes:
  pgdata: